# NIFTY Agent System Requirements
# Install with: pip install -r requirements_agents.txt

# Core dependencies
fastapi>=0.104.0
uvicorn>=0.24.0
pydantic>=2.0.0

# Data fetching
yfinance>=0.2.31
nsetools>=1.0.11
nsepython>=2.0  # Live FII/DII, VIX, events from NSE
feedparser>=6.0.10

# Optional: jugaad-data for RBI rates (may have issues on Windows)
# jugaad-data>=0.25

# Google GenAI for LLM (new SDK - replaces deprecated google-generativeai)
google-genai>=1.0.0

# Supabase client
supabase>=2.0.0

# Data processing
pandas>=2.0.0
numpy>=1.24.0

# Testing
pytest>=7.4.0
pytest-asyncio>=0.21.0
httpx>=0.25.0  # For FastAPI testing

# Finnhub (optional fallback for fundamentals adapter)
finnhub-python>=2.4.0

# Retry logic for rate-limited APIs
tenacity>=8.2.0

# Utilities
python-dotenv>=1.0.0
requests>=2.31.0
